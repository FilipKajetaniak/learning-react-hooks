Add resizing, moving and rotating icons to newElement

Add rotating logic to elements

Include scroll top offset

Move logic for calculating mouse position relative to grid to a reusable function

Split hooks to custom hooks

Enable scrolling on grid and switch from absolute to fixed position in mouse selection

Refactor calculating size code

Handle issues with moving out of grid